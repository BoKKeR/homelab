apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: keyconfig
  namespace: home-automation
spec:
  image: ghcr.io/bokker/keyconfig:master
  interval: 1m
# ---
# apiVersion: image.toolkit.fluxcd.io/v1beta2
# kind: ImagePolicy
# metadata:
#   name: keyconfig
#   namespace: home-automation
# spec:
#   imageRepositoryRef:
#     name: keyconfig
#   policy:
#     semver:
#       range: 5.0.x
---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImageUpdateAutomation
metadata:
  name: keyconfig
  namespace: home-automation
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: home-kubernetes
  git:
    checkout:
      ref:
        branch: main
    commit:
      author:
        email: fluxcdbot@users.noreply.github.com
        name: fluxcdbot
      messageTemplate: 'Automated image update for to'
    push:
      branch: main
  update:
    path: ./kubernetes/apps
    strategy: Setters
