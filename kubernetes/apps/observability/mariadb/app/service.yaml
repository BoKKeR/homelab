apiVersion: v1
kind: Service
metadata:
  name: mariadb-metallb
  annotations:
    metallb.universe.tf/loadBalancerIPs: 10.0.0.20
    metallb.universe.tf/allow-shared-ip: local
spec:
  selector:
    app: mariadb-galera-0
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
  type: LoadBalancer
  loadBalancerIP: 10.0.0.20
