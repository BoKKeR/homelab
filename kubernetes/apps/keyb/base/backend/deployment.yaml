apiVersion: apps/v1
kind: Deployment
metadata:
  name: keyb-backend
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: keyb-backend
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
      imagePullSecrets:
        - name: regcred
