apiVersion: apps/v1
kind: Deployment
metadata:
  name: keyb-sonic
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: keyb-sonic
          imagePullPolicy: IfNotPresent
      restartPolicy: Always
