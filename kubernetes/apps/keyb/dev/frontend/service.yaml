kind: Service
apiVersion: v1
metadata:
  name: keyb-frontend-service
  namespace: keyb
spec:
  selector:
    app: keyb-frontend-dev
  type: ClusterIP
  ports:
    - name: http
      port: 4010
      targetPort: 4010
