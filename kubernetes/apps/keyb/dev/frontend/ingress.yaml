# https://kubernetes.io/docs/concepts/services-networking/ingress/#the-ingress-resource

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: keyb
  name: keyb-frontend-ingress
spec:
  ingressClassName: internal
  rules:
  - host: keyb-dev.tnorbert.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: keyb-frontend-service-dev
            port:
              name: http
