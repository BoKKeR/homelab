apiVersion: apps/v1
kind: Deployment
metadata:
  name: keyb-sonic
  namespace: keyb
spec:
  selector:
    matchLabels:
      app: keyb-sonic-dev
  template:
    metadata:
      labels:
        app: keyb-sonic-dev
    spec:
      containers:
      - name: keyb-sonic
        image: valeriansaliou/sonic:v1.4.9
        ports:
        - name: http
          containerPort: 1491
        envFrom:
          - configMapRef:
              name: keyb-sonic-config-dev
        volumeMounts:
        - mountPath: "/etc"
          subPath: keyb/sonic-dev
          name: unraid-storage
      volumes:
      - name: unraid-storage
        persistentVolumeClaim:
          claimName: unraid-un-nvme-pvc
