kind: Service
apiVersion: v1
metadata:
  namespace: keyb
  name: keyb-frontend-service
spec:
  selector:
    app: keyb-frontend-prod
  type: ClusterIP
  ports:
    - name: http
      port: 4010
      targetPort: 4010
