kind: Service
apiVersion: v1
metadata:
  name: keycloak-steam-provider-service
spec:
  selector:
    app: keycloak-steam-provider
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80
