apiVersion: batch/v1
kind: CronJob
metadata:
  name: curl-cronjob
spec:
  schedule: "0 */8 * * *"  # “At minute 0 past every 8th hour.”
  jobTemplate:
    spec:
      template:
        spec:
          containers:
          - name: curl-container
            image: curlimages/curl:latest  # Using a curl-specific image
            args:
            - /bin/sh
            - -c
            - curl -d '{}' -X POST http://ironcorgi:10050/manual-location-ids-fetch
          restartPolicy: OnFailure
