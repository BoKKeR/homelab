apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageRepository
metadata:
  name: ironcorgi
  namespace: flux-system
spec:
  secretRef:
    name: regcred
  image: ghcr.io/bokker/ironcorgi
  interval: 1m
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: ironcorgi
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: ironcorgi
    namespace: flux-system  # Refer to the ImageRepository in flux-system
  policy:
    semver:
      range: 1.x.x
---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImageUpdateAutomation
metadata:
  name: ironcorgi
  namespace: flux-system
spec:
  interval: 1m
  sourceRef:
    kind: GitRepository
    name: home-kubernetes
    namespace: flux-system  # Reference the GitRepository in flux-system
  git:
    checkout:
      ref:
        branch: master
    commit:
      author:
        email: fluxcdbot@users.noreply.github.com
        name: fluxcdbot
      messageTemplate: 'Automated image update by Flux'
    push:
      branch: master
  update:
    path: ./kubernetes/apps
    strategy: Setters
