---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: speedtest-tracker
  name: speedtest-tracker
spec:
  ports:
  - name: web-ui
    protocol: TCP
    port: 80
    targetPort: 80
  selector:
    app: speedtest-tracker


