---
apiVersion: v1
kind: Service
metadata:
  name: it-tools
  namespace: home-automation
  labels:
    app: it-tools
spec:
  selector:
    app: it-tools
  type: ClusterIP
  ports:
    - protocol: TCP
      name: "webui"
      port: 80
      targetPort: 8080
